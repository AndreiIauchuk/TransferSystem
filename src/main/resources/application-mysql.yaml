spring:
  datasource:
    url: ${MYSQL_URL}
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}
  jpa:
    properties:
      hibernate:
        generate_statistics: false
        show_sql: true

logging:
  level:
    org:
      hibernate:
        type: trace